@using Microsoft.AspNetCore.Identity
@using Team121GBCapstoneProject.Areas.Identity.Data

@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

@{
    ViewData["Title"] = "Chat GPT page";
}

<section class="breadcrumbs">
    <div class="container">
        <h1 class="text-primary">CHAT GPT</h1>
        <div id="chatbot">
            <div id="responses" class="card bluetodarkblue-gradient table-responsive"> 
                @* q: there is not chat div *@
                @* a: the div is in the js file *@
                @* q : on what line? *@
                @* a: line 10 *@
                @* filled in by js *@
            </div>
            <div id="messages">
                <input type="text" id="prompt">
                <button class="btn btn-primary" id="send">Submit</button>
            </div>
        </div>
    </div>
</section><!-- End Breadcrumbs -->


@section Scripts {
    <script type="text/javascript" src="~/js/ChatGpt.js"></script>
}
